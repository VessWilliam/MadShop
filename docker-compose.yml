

services:
   catalogdb:
      image: postgres:18

   catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile
  
   basketdb:
      image: postgres:18
volumes:
   postgres_catalog:
   postgres_basket:
       
